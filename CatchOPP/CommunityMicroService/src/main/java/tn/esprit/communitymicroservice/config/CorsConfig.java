package tn.esprit.communitymicroservice.config;

/**
 * CORS is now handled by SecurityConfig.corsConfigurationSource().
 * This class is kept as a placeholder to avoid breaking imports.
 */
public class CorsConfig {
    // Intentionally empty — see SecurityConfig
}
