package tn.esprit.communitymicroservice.config;

/**
 * JWT utility disabled — security is handled at the API Gateway level.
 * Re-enable by adding spring-boot-starter-security + jjwt to pom.xml.
 */
public class JwtUtil {
    // Intentionally empty
}
