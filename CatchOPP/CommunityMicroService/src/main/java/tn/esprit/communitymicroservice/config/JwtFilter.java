package tn.esprit.communitymicroservice.config;

/**
 * JWT filter disabled — security is handled at the API Gateway level.
 * Re-enable by adding spring-boot-starter-security to pom.xml.
 */
public class JwtFilter {
    // Intentionally empty
}
