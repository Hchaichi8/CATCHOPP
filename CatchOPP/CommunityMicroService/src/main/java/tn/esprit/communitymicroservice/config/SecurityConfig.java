package tn.esprit.communitymicroservice.config;

/**
 * Security is disabled — Spring Security auto-configuration is excluded
 * in CommunityMicroServiceApplication via @SpringBootApplication(exclude = SecurityAutoConfiguration.class)
 * CORS is handled by the API Gateway.
 */
public class SecurityConfig {
    // Intentionally empty
}
